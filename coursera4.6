def computepay(hrs,r):
    return(p)
hrs = float(input("Enter Hours:"))
r = float(input("Enter Rate:"))
##p = computepay
##p = computepay(10,20)
if hrs > 40 :
    p = float((hrs-40) * r * 1.5 + (40 * r))
else :
    p = float(hrs * r)
print("Pay",p)
##computepay(67,418)

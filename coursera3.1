h = float(input("Enter Hours:"))
r = float(input("Enter Rate:"))
if h > 40 :
	pay = float((h-40) * r * 1.5 + (40 * r))
##elif h > 50 :
##  pay = float((h-50) * r * 2.5 + (50 * r))
else :
	pay= float(h * r)
print(pay)
